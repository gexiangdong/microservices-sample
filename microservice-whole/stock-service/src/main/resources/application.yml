server:
  port: 8282 # 服务端口

eureka:
  client:
    service-url:
      defaultZone: http://localhost:1234/eureka/ # 服务注册中心地址
  instance:
    preferIpAddress: true

spring:
  application:
    name: stock-service # 服务名称